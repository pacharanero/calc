{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the calc GUI's main window needs at runtime.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
