#!/usr/bin/env bash
set -euo pipefail

THIS_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$THIS_DIR/.." && pwd)"

PORT=5500

echo "Starting live-server on http://localhost:$PORT"
exec live-server "$ROOT" \
  --port="$PORT" \
  --open="index.html" \
  --no-css-inject
